'use strict'

const User = use('App/Models/User');

class UserController {
    /*async login ({ request, auth }) {
        const { apikey } = request.header['apikey']
        await auth.attempt(apikey)
    
        return await auth.attempt(apikey)
      }*/
}

module.exports = UserController
